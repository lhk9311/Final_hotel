<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminRoom">
	
	<!-- 지점관리자 객실 전체 목록 조회 (지점별) -->
	<select id="getRoomListByManager"
        parameterType="int"
        resultType="RoomTypeDTO">

	SELECT
	    r.ROOM_TYPE_ID AS room_type_id,
	    r.FK_HOTEL_ID AS brand_id,
	    h.HOTEL_NAME AS hotel_name,
	    r.ROOM_GRADE AS room_grade,
	    r.BED_TYPE AS bed_type,
	    r.VIEW_TYPE AS view_type,
	    r.ROOM_NAME AS room_name,
	    r.ROOM_SIZE AS room_size,
	    r.MAX_CAPACITY AS max_capacity,
	    r.TOTAL_COUNT AS total_count,
	    r.BASE_PRICE AS base_price,
	    r.APPROVE_STATUS AS approve_status,
	    i.IMAGE_URL AS image_url
    
	FROM ROOM_TYPE r
	
	JOIN TBL_HOTEL h
		ON r.FK_HOTEL_ID = h.HOTEL_ID
	LEFT JOIN ROOM_IMAGE i
		ON r.ROOM_TYPE_ID = i.ROOM_TYPE_ID
	AND i.IS_MAIN = 'Y'
	
	WHERE r.IS_ACTIVE = 'Y'
	AND h.ADMIN_NO = #{adminNo}
	ORDER BY r.ROOM_NAME
		
	</select>
	
	
	<!-- 운영중 객실 수정 -->
	<update id="modifyApprovedRoom">
		UPDATE room_type
		SET
		    room_name = #{room_name},
		    room_grade = #{room_grade},
		    bed_type = #{bed_type},
		    view_type = #{view_type},
		    base_price = #{base_price},
		    total_count = #{total_count}
		
		WHERE room_type_id = #{roomTypeId}
	</update>
	
	<!-- 이미지 수정시 이거 사용(밑에 있음) -->
    <!-- 	
        <update id="updateRoomImage">
			UPDATE ROOM_IMAGE
			SET IMAGE_URL = #{image_url}
			WHERE ROOM_TYPE_ID = #{roomTypeId}
			AND IS_MAIN = 'Y'
		</update>
	 -->
	
	
	<!-- 객실 비활성화 -->
	<update id="deactivateRoom">
	    UPDATE ROOM_TYPE
	    SET IS_ACTIVE = 'N'
	    WHERE ROOM_TYPE_ID = #{roomTypeId}
	</update>
	
	
	<!-- 비활성 객실 조회 -->
	<select id="getInactiveRoomListByManager"
	        parameterType="int"
	        resultType="RoomTypeDTO">
	
		SELECT
		    r.ROOM_TYPE_ID AS room_type_id,
		    r.FK_HOTEL_ID AS brand_id,
		    h.HOTEL_NAME AS hotel_name,
		    r.ROOM_GRADE AS room_grade,
		    r.BED_TYPE AS bed_type,
		    r.VIEW_TYPE AS view_type,
		    r.ROOM_NAME AS room_name,
		    r.ROOM_SIZE AS room_size,
		    r.MAX_CAPACITY AS max_capacity,
		    r.TOTAL_COUNT AS total_count,
		    r.BASE_PRICE AS base_price,
		    r.APPROVE_STATUS AS approve_status,
		    i.IMAGE_URL AS image_url
		
		FROM ROOM_TYPE r
		
		JOIN TBL_HOTEL h
		ON r.FK_HOTEL_ID = h.HOTEL_ID
		
		LEFT JOIN ROOM_IMAGE i
		ON r.ROOM_TYPE_ID = i.ROOM_TYPE_ID
		AND i.IS_MAIN = 'Y'
		
		WHERE r.IS_ACTIVE = 'N'
		AND h.ADMIN_NO = #{adminNo}
		
		ORDER BY r.ROOM_NAME
	
	</select>
	
	<!-- 객실 복구 -->
	<update id="restoreRoom">
	    UPDATE ROOM_TYPE
	    SET IS_ACTIVE = 'Y'
	    WHERE ROOM_TYPE_ID = #{roomTypeId}
	</update>
	
	
	<!-- 객실 등록 신청 -->	
	
	<!-- 1. 객실 등록(roomtype) -->
	<insert id="insertRoom">

		<selectKey keyProperty="room_type_id" resultType="int" order="AFTER">
		    SELECT seq_room_type.currval FROM dual
		</selectKey>
	
		INSERT INTO room_type
		(
		    room_type_id,
		    fk_hotel_id,
		    room_name,
		    room_grade,
		    room_size,
		    bed_type,
		    view_type,
		    base_price,
		    max_capacity,
		    total_count,
		    approve_status
		)
		VALUES
		(
		    seq_room_type.nextval,
		    
		    (SELECT hotel_id
		     FROM tbl_hotel
		     WHERE admin_no = #{admin_no}),
		     
		    #{room_name},
		    #{room_grade},
		    #{room_size},
		    #{bed_type},
		    #{view_type},
		    #{base_price},
		    #{max_capacity},
		    #{total_count},
		    
		    #{approve_status}
		)
	
	</insert>
		
	<!-- 2. roomtype 이미지 저장 -->
	<insert id="insertRoomImage">	
		INSERT INTO ROOM_IMAGE
		(
		    IMAGE_ID,
		    ROOM_TYPE_ID,
		    IMAGE_URL,
		    IS_MAIN,
		    SORT_ORDER
		)
		VALUES
		(
		    SEQ_ROOM_IMAGE.NEXTVAL,
		    #{fk_room_type_id},
		    #{image_url},
		    'Y',
		    1
		)		
	</insert>
	
	
	<!-- 3. 승인시 roomstock 생성 -->
	<insert id="insertRoomStock365">	
		INSERT INTO ROOM_STOCK (
		    stock_id,
		    room_type_id,
		    stay_date,
		    available_count,
		    is_closed,
		    min_stay
		)
		
		SELECT
		    SEQ_ROOM_STOCK.NEXTVAL,
		    #{roomTypeId},
		    TRUNC(SYSDATE) + (LEVEL - 1),
		    (SELECT total_count
		     FROM ROOM_TYPE
		     WHERE room_type_id = #{roomTypeId}),
		    'N',
		    1
		
		FROM dual
		CONNECT BY LEVEL &lt;= 365	
	</insert>
	
	
	<!-- 반려 후 수정하기 -->
	<!-- 1. 객실 정보 수정 -->
	<update id="updateRoom">
		UPDATE ROOM_TYPE
		SET
		ROOM_NAME = #{room_name},
		ROOM_GRADE = #{room_grade},
		BED_TYPE = #{bed_type},
		VIEW_TYPE = #{view_type},
		BASE_PRICE = #{base_price},
		TOTAL_COUNT = #{total_count}
		WHERE ROOM_TYPE_ID = #{roomTypeId}
	</update>
	
	
	<!-- 2. 객실 이미지 수정  - 이거 안됨 -->
	<update id="updateRoomImage">
		UPDATE ROOM_IMAGE
		SET IMAGE_URL = #{image_url}
		WHERE ROOM_TYPE_ID = #{roomTypeId}
		AND IS_MAIN = 'Y'
	</update>
	
	<!-- 반려 후 재상신 -->
	<update id="resubmitRoom">
		UPDATE ROOM_TYPE
		SET APPROVE_STATUS = 'PENDING'
		WHERE ROOM_TYPE_ID = #{roomTypeId}
	</update>
	
	
	<!-- 지점 관리자용 승인 히스토리 조회 -->
	<select id="getBranchApprovalHistoryList" resultType="map">
		SELECT
		    h.FK_ROOM_TYPE_ID AS room_type_id,
		    rt.ROOM_NAME AS room_name,
		    ho.HOTEL_NAME AS hotel_name,
		    h.STATUS AS status,
		    h.REASON AS reason,
		    h.DECIDED_BY AS decided_by,
		    h.DECIDED_AT AS decided_at
		
		FROM ROOM_APPROVAL_HISTORY h
		
		JOIN ROOM_TYPE rt
		ON h.FK_ROOM_TYPE_ID = rt.ROOM_TYPE_ID
		
		JOIN TBL_HOTEL ho
		ON rt.FK_HOTEL_ID = ho.HOTEL_ID
		
		WHERE ho.ADMIN_NO = #{adminNo}
		
		ORDER BY h.DECIDED_AT DESC
	</select>
	
	
	
	<!-- 총괄관리자 전체 객실 조회 -->
	<select id="getRoomApprovalList" resultType="RoomTypeDTO">	
		SELECT
		    rt.room_type_id,
		    rt.room_name,
		    rt.room_grade,
		    rt.bed_type,
		    rt.view_type,
		    rt.base_price,
		    rt.max_capacity,
		    rt.approve_status,
		    h.hotel_name
		FROM room_type rt
		JOIN tbl_hotel h
		  ON rt.fk_hotel_id = h.hotel_id
		ORDER BY rt.room_type_id DESC
	</select>
	
	
	<!-- 총괄관리자 승인 대기 객실 조회 -->
	<!-- <select id="getPendingRoomList" resultType="RoomTypeDTO">	
		SELECT
		    rt.room_type_id,
		    rt.room_name,
		    rt.room_grade,
		    rt.bed_type,
		    rt.view_type,
		    rt.base_price,
		    rt.max_capacity,
		    rt.approve_status,
		    h.hotel_name
		FROM room_type rt
		JOIN tbl_hotel h
		  ON rt.fk_hotel_id = h.hotel_id
		WHERE rt.approve_status = 'PENDING'
		ORDER BY rt.room_type_id DESC

	</select> -->

	
	<!-- 총괄관리자 객실 승인 처리 -->
	<update id="approveRoom">
		UPDATE room_type
		SET approve_status = 'APPROVED'
		WHERE room_type_id = #{roomTypeId}
	</update>
	
	
	<!-- 총괄관리자 객실 반려 처리 -->
	<update id="rejectRoom">
		UPDATE room_type
		SET approve_status = 'REJECTED'
		WHERE room_type_id = #{roomTypeId}
	</update>
	
	
	<!-- 객실 승인 히스토리 저장 -->
	<insert id="insertApprovalHistory">
		INSERT INTO ROOM_APPROVAL_HISTORY
		(
		    HISTORY_ID,
		    FK_ROOM_TYPE_ID,
		    STATUS,
		    REASON,
		    DECIDED_BY,
		    DECIDED_AT
		)
		VALUES
		(
		    SEQ_ROOM_APPROVAL_HISTORY.NEXTVAL,
		    #{room_type_id},
		    #{status},
		    #{reason, jdbcType=VARCHAR},
		    #{admin_id},
		    SYSDATE
		)
	</insert>
	
	
	<!-- 각 객실 승인 히스토리 조회 -->
	<select id="getRoomApprovalHistory" resultType="map">	
		SELECT
		    h.STATUS AS status,
		    h.REASON AS reason,
		    h.DECIDED_BY AS decided_by,
		    h.DECIDED_AT AS decided_at
		FROM ROOM_APPROVAL_HISTORY h
		WHERE h.FK_ROOM_TYPE_ID = #{roomTypeId}
		ORDER BY h.DECIDED_AT DESC
	</select>
	
	
	
	<!-- 전체 승인 히스토리 조회용 -->
	<select id="getApprovalHistoryList" resultType="map">
		SELECT
		    h.FK_ROOM_TYPE_ID AS room_type_id,
		    rt.ROOM_NAME AS room_name,
		    ho.HOTEL_NAME AS hotel_name,
		    h.STATUS AS status,
		    h.REASON AS reason,
		    h.DECIDED_BY AS decided_by,
		    h.DECIDED_AT AS decided_at
		
		FROM ROOM_APPROVAL_HISTORY h
		
		JOIN ROOM_TYPE rt
		ON h.FK_ROOM_TYPE_ID = rt.ROOM_TYPE_ID
		
		JOIN TBL_HOTEL ho
		ON rt.FK_HOTEL_ID = ho.HOTEL_ID
		
		ORDER BY h.DECIDED_AT DESC
	</select>
	
	<!-- 호텔필터용 -->
	<select id="selectHotelList" resultType="map">
	    SELECT hotel_id, hotel_name
	    FROM tbl_hotel
	    WHERE active_yn = 'Y'
	    ORDER BY hotel_name
	</select>
	

	<!--  엑셀 업로드용
 -->	<insert id="insertRoomFromExcel" parameterType="map">
    INSERT INTO room_type
    (
        room_type_id,
        fk_hotel_id,
        room_name,
        room_grade,
        room_size,
        bed_type,
        view_type,
        base_price,
        max_capacity,
        total_count,
        approve_status,
        is_active
    )
    VALUES
    (
        seq_room_type.nextval,
        #{fkHotelId, jdbcType=NUMERIC},
        #{roomName, jdbcType=VARCHAR},
        #{roomGrade, jdbcType=VARCHAR},
        #{roomSize, jdbcType=NUMERIC},
        #{bedType, jdbcType=VARCHAR},
        #{viewType, jdbcType=VARCHAR},
        #{basePrice, jdbcType=NUMERIC},
        #{maxCapacity, jdbcType=NUMERIC},
        #{totalCount, jdbcType=NUMERIC},
        'PENDING',
        'Y'
    )
</insert>

	
</mapper>